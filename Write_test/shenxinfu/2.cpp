// 0285289430826
// 2
// 0 2
// 2 3

#include <iostream>
#include <array>
#include <vector>
#include <string>
#include <map>
using namespace std;

int main()
{
    string str;
    cin >> str;
    long n;
    cin >> n;
    map<char,char> mapping;
    for (int i = 0; i < n; i++)
    {
        char left, right;
        cin >> left >> right;
        if(mapping.find(left)）{
            continue;
        }else{
            for(char key;mapping)
        }
        cout<<left;
    }
    return 0;
}